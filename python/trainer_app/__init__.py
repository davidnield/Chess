"""Local opening-training app: Explorer / Trainer / Deviations.

Run with:  python -m trainer_app --data <dir>     (see __main__.py)
The repo's python/ dir must be on sys.path (run_trainer.bat sets PYTHONPATH)
so shared modules (zobrist, repertoire_explorer helpers) import cleanly.
"""

__version__ = "0.1.0"
