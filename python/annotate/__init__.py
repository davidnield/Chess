"""Repertoire annotation pipeline: deterministic fact sheets + targeted engine
analysis + Claude-generated course prose. See the plan in the repo docs and
each module's docstring. Runs against the trainer app's training pack."""
