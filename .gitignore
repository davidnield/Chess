.Rproj.user
.Rhistory
.Rdata
.httr-oauth
.DS_Store
.quarto
data/
.claude/
.venv/
__pycache__/
*.pyc
logs/

# Notification credentials — never commit. Contains SMTP password + email.
.chess_pipeline_notify.json
chess_pipeline_notify.json
python/notify_config.json
python/notify_config.local.json
.env
.env.local
# Opening-training app user data (FSRS history, imported games, training pack)
trainer_data/

# Ad-hoc one-off scripts: benchmark harnesses, drivers for runs that have already
# completed, throwaway probes. Kept on disk because they document how a past result
# was produced, but deliberately not pushed — they are not part of the pipeline or
# the test suite and would only be noise in a fresh checkout. See scratch/README.md.
scratch/
